- 前端组件与工具通过 shadcn/cli 安装，统一落至 @/shared/components、@/shared/utils、@/shared/hooks 并通过 alias 暴露 ui/lib/hooks 入口
- 所有子模块共享同一份 tailwind.config.js 与 globals.css，采用 neutral baseColor 的黑白主体视觉规范
- 跨进程通信仅通过 /api 前缀的 REST 调用，由 Vite proxy 转发至 NestJS，避免硬编码后端地址