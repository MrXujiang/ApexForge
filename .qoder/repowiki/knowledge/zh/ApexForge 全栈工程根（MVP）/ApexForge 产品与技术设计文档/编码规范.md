- 所有架构图使用 Mermaid 语法内嵌在 Markdown 中（graph TD / stateDiagram-v2 / sequenceDiagram），避免外部图片依赖。
- 数据库表与 API 响应体以表格形式定义字段名、类型与说明，保持数据结构与接口契约的可读性。
- MVP 与平台化两套选型并列呈现，并在演进策略中明确迁移路径（如 SQLite→PostgreSQL、内存缓存→Redis）。