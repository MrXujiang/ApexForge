React + TypeScript；UI 采用 Radix Primitives (`@radix-ui/react-slot`) 配合 class-variance-authority 实现 shadcn 风格组件；样式系统基于 TailwindCSS，类名合并通过 clsx + tailwind-merge；HTTP 客户端使用 Vite 原生 `import.meta.env` 注入 `VITE_API_BASE_URL`。