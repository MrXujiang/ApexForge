- 所有对外响应统一包裹为 `{ traceId: createTraceId(), data: ... }` 结构，便于链路追踪。
- 数据库中的 JSON 列统一通过 `common/json` 的 `stringifyJson`/`parseJson` 进行序列化与反序列化，避免裸 JSON 字符串。
- 业务实体 ID 通过 `createId(prefix)` 生成（如 'asset'、'ver'），而非依赖自增主键。
- 资源不存在时抛出 `NotFoundException({ code, message })` 并携带结构化错误码，而非通用消息。