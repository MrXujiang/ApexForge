- 每个业务域采用 `modules/<domain>/` 目录组织，内部按 controller/service/dto 三层拆分并配套同名 `*.module.ts` 声明依赖
- 对外请求体一律使用 class-based DTO（位于 `dto/` 子目录）配合类验证器进行入参校验
- 数据库访问统一通过 `prisma/PrismaService` 单例注入，禁止在各模块内直接 new PrismaClient